export interface createUserProps {
    username: string;
    email: string;
    fullName: string;
    password: string;
  }

  export interface LoginProps {
    identifier: string;
    password: string;
  }